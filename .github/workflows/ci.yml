name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run BackEnd / FrontEnd- NodeJs - Quasar
      run: |
        cd FrontEnd
        sudo npm install -g @quasar/cli
        npm install
        quasar build 